namespace UILib {
    /**
     * <summary>
     * Scroll types which can be applied to ScrollBars.
     * </summary>
     */
    internal enum ScrollType {
        None,
        Vertical,
        Horizontal,
    }
}
