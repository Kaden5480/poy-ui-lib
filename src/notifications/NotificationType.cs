namespace UILib.Notifications {
    /**
     * <summary>
     * The types of notifications which
     * can be displayed to the user.
     * </summary>
     */
    public enum NotificationType {
        Silent,
        Normal,
        Error,
    }
}
